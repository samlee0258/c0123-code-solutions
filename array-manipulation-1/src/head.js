/* exported head */
/*
  - define a function named head with one parameter "array"
  - get the first index value of the given array
  - return output
*/

function head(array) {
  var firstIndex = array[0];
  return firstIndex;
}
