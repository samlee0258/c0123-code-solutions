/* exported getElementAtIndex */
/*
  - create a function named getElementAtIndex with two parameters "array" and "index"
  - get a specific element in the array using array[index] and assign it to a variable
  - return the value of the variable
*/

function getElementAtIndex(array, index) {
  var indexValue = array[index];
  return indexValue;
}
