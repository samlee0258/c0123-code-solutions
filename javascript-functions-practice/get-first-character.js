/* exported getFirstCharacter */
/*
  - create a function named getFirstCharacter with one parameter "string"
  - using the charAt method get the first character of the string and assign it
  to a variable
  - return the value of the variable
*/

function getFirstCharacter(string) {
  var firstCharacter = string.charAt(0);
  return firstCharacter;
}
