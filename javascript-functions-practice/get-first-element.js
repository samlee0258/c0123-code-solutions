/* exported getFirstElement */
/*
  - create a function named getFirstElement with one parameter "array"
  - get the first index of the array and assign it to a variable
  - return the value of the variable
*/

function getFirstElement(array) {
  var firstIndex = array[0];
  return firstIndex;
}
