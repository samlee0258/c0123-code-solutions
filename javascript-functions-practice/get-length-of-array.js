/* exported getLengthOfArray */
/*
  - create a function named getLengthOfArray with one parameter "array"
  - using the .length method find the length of the array and assign it to a variable
  - return the value of the variable
*/

function getLengthOfArray(array) {
  var length = array.length;
  return length;
}
