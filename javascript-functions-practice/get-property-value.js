/* exported getPropertyValue */
/*
  - create a function named getPropertyValue with two parameters "object" and "key"
  - using the bracket notation get the value of the property at the object[key] location
  and assign it to a variable
  - return the value of the variable
*/

function getPropertyValue(object, key) {
  var value = object[key];
  return value;
}
