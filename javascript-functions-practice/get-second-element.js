/* exported getSecondElement */
/*
  - create a function named getSecondElement with one parameter "array"
  - get the second index in the array and assign it to a variable
  - return the value of the variable
*/

function getSecondElement(array) {
  var secondIndex = array[1];
  return secondIndex;
}
