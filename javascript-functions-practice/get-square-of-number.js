/* exported getSquareOfNumber */
/*
  - create a function named getSquareOfNumber with one parameter "number"
  - multiply number with itself and assign the result of that expression to a variable
  - return value of variable
*/

function getSquareOfNumber(number) {
  var square = number * number;
  return square;
}
