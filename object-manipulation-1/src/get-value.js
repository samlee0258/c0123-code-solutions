/* exported getValue */
/*
  - define a function named getValue with two parameter "object" and "key"
  - assign the object at key to a variable named value
  - return the value of the property object
*/

function getValue(object, key) {
  var value = object[key];
  return value;
}
