/* exported setValue */
/*
  - define a function named setValue with three parameters "object", "key", and "value"
  - use bracket notation on the object at the key and assign it the value to create
    a new property with a value for the object
*/

function setValue(object, key, value) {
  object[key] = value;
}
